[Unit]
Description=Unix socket for relaying gpg4win gpg-agent.extra socket

[Socket]
ListenStream=%t/gnupg/S.gpg-agent.extra
SocketMode=0600
DirectoryMode=0700
Accept=true

[Install]
WantedBy=sockets.target
