DEPENDENCIES="build-essential clang-format diffutils dos2unix \
 libhidapi-hidraw0 python3 pipx unzip wget zip zstd"

if [ "$(uname -m)" == "aarch64" ]
then
  DEPENDENCIES="$DEPENDENCIES gcc-arm-none-eabi gcc-avr avrdude \
   dfu-programmer dfu-util"
fi

PACKAGES="uv qmk"
