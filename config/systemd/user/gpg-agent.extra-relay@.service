[Unit]
Description=gpg4win to WSL connector for SSH
Requires=gpg-agent.extra-relay.socket

[Service]
Type=simple
ExecStart=%h/.local/bin/npiperelay.exe -ep -ei -s -a 'C:/Users/hmorris/AppData/Local/gnupg/S.gpg-agent.extra'
StandardInput=socket

[Install]
WantedBy=default.target
